'use client';
export default function Error({ }) {
    return (
        <main className="error">
            <h1>An Error occured!</h1>
            <p> One or more fields are missing or you are not using the right format.</p>
        </main>
    );
}